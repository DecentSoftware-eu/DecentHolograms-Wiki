---
title: '/dh hologram disable'
description: 'Disables a Hologram'
---

The `disable` subcommand is used to disable a Hologram.  
Unlike the [`delete` subcommand](delete.md) does disabling a Hologram not delete it.

{{ permissions("hologram", "disable") }}

{{ aliases(["off"]) }}

## Arguments

### `#!command <hologram>`

Name of the Hologram to disable.