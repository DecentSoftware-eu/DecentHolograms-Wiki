---
title: '/dh pages'
description: 'Allows the edit of Hologram pages'

template: index_list.html
---

The `pages` command allows the adding, editing and deletion of Hologram pages.

{{ permissions("pages") }}

{{ aliases(["page", "p"]) }}

## Subcommands