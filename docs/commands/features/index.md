---
title: '/dh features'
description: 'Allows the enabling/disabling of Features in the plugin'

template: index_list.html
---

The `features` command provides a set of sub-commands that can be used to enable or disable Features in the plugin.

{{ permissions("features") }}

{{ aliases(["feature", "f"]) }}

## Subcommands