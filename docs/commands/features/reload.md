---
title: '/dh features reload'
description: 'Reloads a feature'
---

The `reload` subcommand allows to reload a specific feature.

{{ permissions("features", "reload") }}

## Arguments

### `#!command <feature>`

Name of the feature to reload.